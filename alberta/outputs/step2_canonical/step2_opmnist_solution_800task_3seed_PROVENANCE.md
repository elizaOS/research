# Step 2 OPMNIST 800-task 3-seed closure — vendored snapshot provenance

Development-grade, nonpromoting snapshot. These files were vendored on
2026-08-02 (UTC) from the upstream execution tree, byte-identical to their
sources (see sha256 below). They are the merged result and solution-gate
audit of the three-seed, published-scale OPMNIST closure run described in
`OPMNIST_DEVELOPMENT_INGESTION.md` and the upstream runbook.

## Source of the run

- Execution tree: `/home/shaw/milady/alberta` (upstream `alberta_framework`
  pinned via `PYTHONPATH`; the runner, merge, and gate scripts live in its
  `benchmarks/` and `examples/The Alberta Plan/Step2/` trees, which this
  standalone research repo deliberately omits — see `VENDORING.md`).
- Runbook: `outputs/step2_opmnist_solution_full/RUNBOOK.md` (upstream tree),
  plan `step2_opmnist_solution_800task_3seed_plan.json` generated by
  `benchmarks/step2_opmnist_full_run_plan.py --n-seeds 3`.
- Protocol: OpenML MNIST canonical 60000/10000 split, 800 permutations x
  60000-step task blocks = 48,000,000 online updates per seed, prediction
  before update every step, no task id, held-out evaluation over all 800
  permutation views. Seeds 0, 1, 2 (launched 2026-07-31, completed
  2026-08-02 UTC).
- Merge: `benchmarks/step2_opmnist_merge_seed_results.py` over the three
  seed-split result JSONs in
  `outputs/step2_opmnist_solution_full/seed_splits/`.
- Gate: `benchmarks/step2_opmnist_solution_gate.py --min-seeds 3
  --allow-unsolved` (exit 0 with the diagnostic flag; the promotion-mode
  gate without `--allow-unsolved` would exit 2 because
  `solved_opmnist_step2=false`).

## Vendored files (byte-identical copies)

| File | sha256 |
|---|---|
| `step2_opmnist_solution_800task_3seed_results.json` | `7bf3cc321df8eaac4f61ce6d5efbf801440c78f5e7b99725d3b760621d55919a` |
| `step2_opmnist_solution_800task_3seed_solution_gate.json` | `806ebe6ec69cf489fb11fdab3c9227e1d0bdb77fb809fecbd6d01412e5d22c7e` |
| `step2_opmnist_solution_800task_3seed_SUMMARY.md` | `aaec495f226aa19322a08a14fec1a5f51f73c1f2bbb13e779c70036ccef625b4` |

Source paths (upstream tree):
`/home/shaw/milady/alberta/outputs/step2_opmnist_solution_full/step2_opmnist_solution_800task_3seed_{results.json,solution_gate.json,SUMMARY.md}`.

## Frozen gate outcome (schema `alberta.step2.opmnist_solution_status.v1`)

- `protocol_complete=true`, `multi_seed_full_scale=true`,
  `configured_seed_count=3`, `completed_record_seed_count=3`,
  `provenance_complete=true` (merged-split manifest).
- `solved_opmnist_step2=false`,
  `claim_scope="limited_opmnist_evidence_not_step2_solution"`,
  `candidates_winning_all_metrics=[]`.
- Metric wins vs the best fair MLP (both candidates,
  `step2_hybrid_memory_trace` and
  `step2_hybrid_memory_trace_adaptive_sharp`): win `online_mean_mse`,
  `online_mean_accuracy`, `final_window_mse`, `final_window_accuracy`;
  lose `test_mse`, `test_accuracy`.

This is the multi-seed confirmation-at-scale boundary: 3 completed seeds x
800 true-MNIST task blocks x all 800 held-out views. It is NOT a
solved-Step-2 claim; the open boundary remains all-metric closure
(`test_mse`/`test_accuracy` still favor the fair MLPs). The prior 1-seed
conclusion (mixed metric wins, candidates strong on online/tracking metrics,
fair MLPs retaining the held-out test metrics) held at 3 seeds, with one
refinement: `final_window_accuracy` — a fair-MLP win in the 1-seed artifact —
flips to the candidates at 3 seeds (`adaptive_sharp` 3/0, primary 2/1 vs
`mlp_h128`).

Immutability: these vendored files are frozen snapshots. Never edit or
overwrite them; any rerun writes to NEW paths.
