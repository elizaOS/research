{
  "name": "@elizaos/research",
  "private": true,
  "type": "module",
  "description": "elizaOS research monorepo: chip, robot, alberta, and the AiNex plugin. This root defines a Bun workspace over the self-contained TypeScript surfaces (plugin-ainex, robot) and pins the shared toolchain (TypeScript, Biome, Vitest).",
  "license": "MIT",
  "workspaces": [
    "plugin-ainex",
    "robot"
  ],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.3",
    "@types/ws": "^8.5.12",
    "@typescript/native-preview": "7.0.0-dev.20260703.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
